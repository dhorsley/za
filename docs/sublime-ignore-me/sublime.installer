#!/usr/bin/bash

which batcat >/dev/null 2>&1

if (($? != 0)); then
  # alias bat=batcat
  echo "batcat not installed."
  exit 1
fi

p=$(batcat --config-dir)
mkdir -p ${p}/syntaxes
cp za.sublime-syntax ${p}/syntaxes/
batcat cache --build
