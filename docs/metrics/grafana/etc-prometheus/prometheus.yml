# my global config
global:
  scrape_interval: 15s # Set the scrape interval to every 15 seconds. Default is every 1 minute.
  evaluation_interval: 15s # Evaluate rules every 15 seconds. The default is every 1 minute.
  # scrape_timeout is set to the global default (10s).

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
          # - alertmanager:9093

# Load rules once and periodically evaluate them according to the global 'evaluation_interval'.
rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

# A scrape configuration containing exactly one endpoint to scrape:
# Here it's Prometheus itself.
scrape_configs:
  # The job name is added as a label `job=<job_name>` to any timeseries scraped from this config.
  - job_name: "prometheus"

    # metrics_path defaults to '/metrics'
    # scheme defaults to 'http'.

    static_configs:
      - targets: ["localhost:9090"]
       # The label name is added as a label `label_name=<label_value>` to any timeseries scraped from this config.
        labels:
          app: "prometheus"
  - job_name: "za"
    # Za Prometheus exporter (requires ZA_PROMETHEUS_CIDR configured on za server)
    # The scraping IP must be included in the ZA_PROMETHEUS_CIDR allowlist, e.g.:
    #   export ZA_PROMETHEUS_CIDR="192.168.1.100/32"  (single Prometheus server)
    #   export ZA_PROMETHEUS_CIDR="192.168.0.0/16"     (entire subnet)
    #   export ZA_PROMETHEUS_CIDR="0.0.0.0/0"           (all IPv4 + auto-expanded IPv6)
    static_configs:
      - targets: ["192.168.1.19:9091"]
        labels:
          app: "za"

