println "Debug dirent test"
files = dir(".")
println "Files count: ", len(files)
if len(files) > 0
    println "First file name: ", files[0].Name
    println "First file size: ", files[0].Size
    filtered_files = files ?> "#.Size > 0"
    println "Filtered files count: ", len(filtered_files)
endif