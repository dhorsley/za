# Test: Mixed enums should error
enum Status (pending, running, completed, failed)
enum Colour (red, green, blue)
task_status = Status.running

case task_status full
is Status.pending
    println "pending"
is Colour.red
    println "red - wrong enum!"
endcase
