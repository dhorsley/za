#!/usr/bin/env za

println("Testing basic functionality")

# Test 1: Simple array creation
var basic []any
basic = [1, 2, 3, 4, 5]
println("Basic array length: " + str(len(basic)))

# Test 2: Try list_int conversion
var ints []int
ints = [1, 2, 3, 4, 5].list_int
println("Int array length: " + str(len(ints)))

# Test 3: Try big int conversion
var big_ints []bigi
big_ints = [1, 2, 3, 4, 5].list_bigi
println("Big int array length: " + str(len(big_ints)))

if len(big_ints) > 0 {
    println("First big int: " + big_ints[0].String())
}

println("All tests completed!")