# Debug what $idx replacement produces
numbers = [10, 20, 30]

# Test what the expression becomes after replacement
# This should show us the issue
expr = "item_$idx"
println "Original expression: ", expr

# Try to see what happens after replacement
# We'll manually do what our code does
replaced = "item_0"
println "After replacement: ", replaced

# Try to evaluate this as expression (this should fail)
try
  result = replaced
  println "Evaluation result: ", result
catch
  println "Evaluation failed as expected"
endtry