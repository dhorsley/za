#!/usr/bin/za

tc=0
interpol(false)
foreach l in $in "/dev/stdin"
  c=l.tokens.tokens.len
  print "\n[#2]%5d[#-]".format(1+key_l)
  continue if c==0
  print " : [#4]%3d[#-] : %s".format(c,l.stripcc)
  tc+=c
endfor

println "\n\nTotal tokens in file : ",tc,"\n"

