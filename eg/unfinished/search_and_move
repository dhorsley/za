#!/usr/bin/za -f

input filter param 1
input destination param 2

l =| file *

foreach q in l
    if match(q,filter) 
        fields(q)
        z=replace(F1,":$","")
        | mv -f {z} {destination}/"
    endif
endfor

