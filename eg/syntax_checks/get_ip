#!/usr/bin/za

ip=web_get("https://api.ipify.org/?format=text")

if len(ip)==0
    println "Download error."
    exit 1
endif

octet=split(ip[0],".")
println "octets : ",octet

total=sum(list_int(octet))

if total > 100
  println "The sum of {ip[0]} is greater than 100: {total}."
endif


