#!/usr/bin/za

#
# this will be almost the same as a bash example.
# we do not carry any mail functions.
#

#
# you must have the mailutils, or similar, package for this to work.

rcpt="admin@example.com"
subj="greeting"
body="welcome to our site!"
| echo "{body}" | mail -s {subj} {rcpt}
println "Error number : ",last()
println "Error Content: ",last_out()

