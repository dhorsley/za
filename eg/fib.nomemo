#!/usr/bin/za

define fib(n)
    # setglob calls = getglob("calls")+1
    on n<=1 do return n
    return fib(n-1)+fib(n-2)
enddef

#define signum(x)
#    if x<0 ; return -1 ; endif
#    if x>0 ; return 1 ; endif
#    return 0
#enddef

# main

input x param 1
# calls=0
println fib(x)
# println "calls -> {calls}"

#for x=0 to 100
#    on signum(fib(x))==-1 do break
#    println "fib({x}) -> {fib(x)}"
#endfor


