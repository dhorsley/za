#!/usr/bin/za

define fib(n)
    setglob calls++
#    println thisref(), " ref and mem-> ", getmem()
    on n<=1 do return n
    return fib(n-1)+fib(n-2)
enddef

calls=0
input x param 1
println fib(x)
println "calls -> {calls}"


