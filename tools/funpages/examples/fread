# Example for fread
try
    write_file("/tmp/test.txt", "hello world")
    fp = fopen("/tmp/test.txt", "r")
    println fread(fp, 5)
    fclose(fp)
catch err
    println "File operation failed"
endtry
