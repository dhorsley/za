# Example for smtp_send_with_auth
try
    smtp_send_with_auth("localhost:25", "25", "user", "pass", list_string(["from@a.com", "to@b.com"]), "hello", "subject")
catch err
    println "SMTP connection failed (expected)"
endtry
